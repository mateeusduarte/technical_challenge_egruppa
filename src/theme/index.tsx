import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#e62f40",
      main: "#e62f40",
      dark: "#e62f40",
      contrastText: "#e62f40",
    },
    secondary: {
      light: "#000",
      main: "#000",
      dark: "#000",
      contrastText: "#000",
    },
  },
});

export default theme;
